export class NavOption {
    // text: string;
    // route: string;
    // selected: boolean;

    constructor(public id: number, public text: string, public route: string, public selected: boolean) {}
}
